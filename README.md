# StackCompiler

Простой и удобный инструмент командной строки (CLI) на Node.js для автоматической генерации файлов `docker-compose.yml`.

## Возможности

* 🖥 **Интерактивный интерфейс:** Выбор сервисов через удобное меню в терминале.
* ⚙️ **Настройка параметров:** Возможность задать порты, пароли, имена баз данных и пользователей для каждого сервиса.
* 🐳 **Поддержка популярных сервисов:** Nginx, MySQL (latest), PostgreSQL, Redis, MongoDB, phpMyAdmin.
* 📄 **Генерация YAML:** Создает корректный и валидный `docker-compose.yml` в текущей директории.
* 💾 **Автоматические Volumes:** Автоматически прописывает именованные тома (volumes) для персистентности данных.

## Установка

1.  Убедитесь, что у вас установлен [Node.js](https://nodejs.org/) (рекомендуется версия 16+).
2.  Скачайте исходный код проекта.
3.  Откройте терминал в папке проекта и установите зависимости:

```bash
npm install
```

Использование:
```bash
npm start
```
Процесс работы
Выбор сервисов: Используйте стрелки для навигации, Пробел для выбора сервисов и Enter для подтверждения.

Конфигурация: Ответьте на вопросы по каждому выбранному сервису (порты, доступы).

Результат: В корневой папке будет создан файл docker-compose.yml.

Добавление новых сервисов
Проект легко расширяем. Чтобы добавить свой шаблон сервиса:

Откройте файл serviceConfig.js.

Добавьте название сервиса в массив availableServices.

Опишите логику генерации и вопросы в объекте serviceTemplates.
